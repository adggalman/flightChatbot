# Copy this file to .env and fill in real values
# Never commit .env to git

# ── Web Search (Tavily) ──────────────────────────────
# Used by: Gemini CLI (run_shell_command curl)
# Sign up: https://app.tavily.com
TAVILY_API_KEY=

# ── MCP Servers (Claude Code) ────────────────────────
# Configured in: ~/.claude/settings.json (global, outside repo)
# See: docs/MCP_SETUP.md for full config blocks
.e