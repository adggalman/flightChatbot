{
    "version": 2,
    "ignoreCommand": "git log --name-only --format='' -1 | grep -q '^backend/'",
    "builds": [
      {
        "src": "server.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "server.js"
      }
    ]
}